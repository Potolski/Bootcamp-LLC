[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.localnet]
redbutton = "G96NupAUcEpd284gAN7FN5ZZiQRCnz36uf5bvVmcveCG"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = ".secrets/deployer.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
